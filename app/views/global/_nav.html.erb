<nav class="global-nav navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Vocal Local</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div>
      <ul class="nav navbar-nav">
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control global-search" placeholder="Search Issues and Towns">
        </div>
        <button type="submit" class="btn btn-default">Search</button>
      </form>

      <li class=""><a href="#">My Town</a></li>

      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">User (<%= h authenticated_user.email %>) <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to('My Account', edit_user_path(authenticated_user))%>
            <li class="divider"></li>
            <li><%= link_to 'Logout', session_path, method: :delete %></li>
          </ul>
        </li>

        <% else %>
        <li><a href="/session/login">Login</a></li>
        <% end %>

      </ul>

    </div>
  </div>
</nav>