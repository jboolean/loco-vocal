<nav class="global-nav navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Vocal Local</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div>
      <ul class="nav navbar-nav">
      <form class="navbar-form navbar-left" role="search" action="/towns/search" method="GET">
        <div class="form-group">
          <input name="query" type="text" class="form-control global-search" placeholder="Search Issues and Towns">
        </div>
        <button type="submit" class="btn btn-default">Search</button>
      </form>

      <% unless !logged_in? || authenticated_user.my_town.nil? %>
      <li class="my-town-btn"><%= link_to(authenticated_user.my_town.name, town_path(authenticated_user.my_town)) %></li>
      <% end %>

      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">User (<%= h authenticated_user.email %>) <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to('My Account', edit_user_path(authenticated_user))%>
            <li class="divider"></li>
            <li><%= link_to 'Logout', delete_session_path %></li>
          </ul>
        </li>

        <% else %>
        <li><a href="/session/login">Login</a></li>
        <li class="register-btn"><a href="/users/new">Register</a></li>
        <% end %>

      </ul>

    </div>
  </div>
</nav>